Project from 9/5/2025<br>
I tried to make it so the code would choose between a random number generator between 1 and 5, so I used a random number generator and leds with different melodies to represent each hand. Unfortunately, it doesn’t work as intended as if you do blow onto it, it doesn’t play a random number. It instead loops the numbers in a random order.
